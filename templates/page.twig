{% extends "base.twig" %}

{% block content %}
	<div class="container pt-5 pb-5">
		<div class="row">
			<div class="col-12">
				<h1>{{ post.title }}</h1>
				<div class="mt-5">{{ post.content }}</div>
			</div>
		</div>
	</div>
{% endblock %}
